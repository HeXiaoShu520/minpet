# miniPet 桌面 AI 宠物体验设想

本文不是普通消息通知方案。miniPet 不应该把飞书通知换一层皮，也不应该变成一个小号飞书客户端。

miniPet 的价值是：**让 AI 在桌面上有一个可见、可互动、可陪伴、可确认、可投喂的身体。**

一句话：

> 飞书负责消息流，miniPet 负责 AI 判断后的关键时刻。

---

## 1. 核心定位

miniPet 不是通知中心，而是桌面 AI Runtime。

它应该做这些事：

- 帮用户判断什么值得打扰。
- 在用户需要决策时出现。
- 在 AI 做长任务时给用户一个可见状态。
- 在 AI 缺上下文时主动追问。
- 在高风险操作前用桌面确认降低误操作。
- 接受用户从桌面投喂文本、文件、截图、链接。
- 用宠物动作、情绪、声音表达 AI 当前状态。

它不应该做这些事：

- 普通飞书消息通知。
- 普通日程提醒。
- 普通审批红点。
- 普通群消息转发。
- 完整聊天客户端。
- 文件下载器。
- 会话列表和已读未读管理。

---

## 2. 和飞书通知的区别

| 场景 | 飞书通知 | miniPet |
| --- | --- | --- |
| 普通消息来了 | 弹通知 | 不处理 |
| 群里有人 @ 我 | 弹通知 | AI 判断是否需要回复，必要时给出建议 |
| 领导问进度 | 弹原消息 | 生成回复草稿，让用户确认 |
| 一堆群聊讨论 | 用户自己看 | AI 汇总成 3 条待处理事项 |
| 会议结束 | 会议纪要在飞书里 | AI 提炼待办，问用户是否创建任务 |
| 用户拖文件到桌面 | 飞书不处理 | miniPet 接收投喂，交给后端总结/发起任务 |
| AI 正在跑长任务 | 没有存在感 | 桌宠表现“思考/工作/等待确认” |
| 高风险操作 | 可能在后端直接执行 | 桌面二次确认，明确展示将要做什么 |

核心差异：

> 飞书通知告诉你“发生了什么”；miniPet 应该告诉你“你现在要不要做什么”。

---

## 3. 值得让 miniPet 出现的时刻

### 3.1 AI 判断需要用户处理

不是每条消息都弹，而是后端先判断：

- 这条消息是否真的和用户有关？
- 是否需要用户回复？
- 是否有截止时间？
- 是否有风险？
- 是否能直接给出建议？

示例：

```text
产品群里 20 条消息不弹。
AI 判断其中 1 条在问你“下午四点方案能不能给”。
miniPet 出现：
“产品群有一条消息可能需要你回复，我起草了一版。”
[发送] [修改] [忽略]
```

这才是桌宠价值。

---

### 3.2 AI 生成草稿，需要用户确认

适合：

- 回复飞书消息。
- 发送邮件。
- 创建日程。
- 创建任务。
- 修改文档。
- 批量操作。

体验：

```text
宠物探头：
“我帮你写好了，要发吗？”

卡片：
收件人：产品群
内容：可以，我四点前发初版。

[确认发送] [修改语气] [取消]
```

重点不是通知，而是**确认闭环**。

---

### 3.3 AI 缺少上下文，桌面追问

传统后端任务失败时，用户可能根本不知道缺什么。

miniPet 可以让 AI 在桌面上追问：

```text
“你说的‘发给客户’，是哪个客户群？”

[甲方项目群]
[售前沟通群]
[手动输入]
```

这让桌宠成为 AI 的交互入口，而不是消息气泡。

---

### 3.4 长任务可视化

适合：

- 整理会议纪要。
- 汇总一天未读。
- 搜索相关文档。
- 生成项目周报。
- 批量拆任务。

体验：

```text
宠物进入工作状态。

正在整理昨天会议：
✓ 读取会议纪要
✓ 提取待办
● 匹配负责人
○ 等待你确认是否创建任务
```

用户不需要打开后台页面，就知道 AI 没卡死。

---

### 3.5 桌面投喂

这是桌宠相比飞书通知最有想象力的能力。

用户可以把内容直接拖给宠物：

- 文本片段。
- 本地文件。
- 截图。
- 飞书文档链接。
- 网页链接。
- 图片。

然后宠物问：

```text
“这个要我怎么处理？”

[总结]
[变成待办]
[起草回复]
[发到飞书]
[问 AI]
```

这不是消息通知，这是桌面原生入口。

---

### 3.6 情绪和动作表达 AI 状态

普通通知是静态的，桌宠可以有状态。

| AI 状态 | 宠物表现 |
| --- | --- |
| 空闲 | 待机、眨眼 |
| 正在听 | 靠近、耳朵动 |
| 思考中 | 歪头、转圈 |
| 工作中 | 敲键盘、搬东西 |
| 等用户 | 举牌、看向用户 |
| 完成 | 开心、跳一下 |
| 失败 | 摔倒、委屈 |
| 紧急 | 轻推、明显动作 |

这会让 AI 从“后台接口”变成“桌面角色”。

---

## 4. 几个让人眼前一亮的场景

### 场景 A：智能挡通知

飞书消息很多，但 miniPet 不吵。

它只在 AI 判断“这件事真的需要你处理”时出现：

```text
“我看了刚刚产品群的讨论，只有一件事需要你拍板：
接口方案今天是否先按 A 版推进？”

[A 版推进]
[让我看看上下文]
[稍后]
```

价值：从消息噪音变成决策入口。

---

### 场景 B：自动起草回复

```text
宠物：
“张三在问你四点能不能给方案。我帮你写了个简短回复。”

建议回复：
可以，我四点前发初版，有变动我提前同步。

[发送]
[更正式]
[更口语]
[忽略]
```

用户点“更正式”后，后端重新生成，再让用户确认。

---

### 场景 C：会议后自动收口

会议结束后不弹普通通知，而是等 AI 整理完：

```text
“刚才会议我整理出 3 个和你有关的待办，要放进飞书任务吗？”

1. 补接口文档，今天 18:00 前
2. 确认排期，明天上午
3. 给客户发版本说明

[全部创建]
[逐条确认]
[忽略]
```

价值：从会议纪要变成行动。

---

### 场景 D：桌面截图投喂

用户截图拖给宠物：

```text
“这张图要我做什么？”

[识别文字]
[总结问题]
[发给某人]
[生成任务]
```

如果选“发给某人”：

```text
“发给谁？”
[产品群]
[研发群]
[手动输入]
```

最后展示发送确认。

---

### 场景 E：AI 正在替你跑腿

用户说：

```text
帮我查一下上周关于 miniPet 协议的讨论，整理成一页总结。
```

miniPet 显示：

```text
我在找：
✓ 搜索飞书消息
✓ 搜索相关文档
● 汇总关键结论
○ 生成摘要卡片
```

完成后：

```text
“我整理好了，要打开结果吗？”
[查看摘要]
[保存到飞书文档]
[发给项目群]
```

---

### 场景 F：专注模式下的“只打扰重要事”

用户进入专注模式后：

- 普通消息不弹。
- 普通日程不弹。
- 只有 AI 判断紧急且需要用户处理的事项才让宠物轻推。

```text
“抱歉打扰，这个可能需要你现在确认：生产发布是否继续？”

[继续]
[暂停]
[查看原因]
```

这比普通通知更克制，也更可信。

---

## 5. 对端后端应该怎么设计

对端不要把飞书原生消息直接转发给 miniPet。

应该做一层判断和翻译：

```text
飞书原生事件
  ↓
解析消息内容
  ↓
补全上下文：群名、发送人、thread、历史消息
  ↓
AI/规则判断是否值得打扰
  ↓
生成交互意图
  ↓
发送 miniPet 协议
```

推荐输出不是“消息通知”，而是这些交互意图：

| 后端意图 | miniPet 协议 |
| --- | --- |
| 展示关键消息 | `display.message.show` |
| 展示总结结果 | `display.card.show` |
| 确认高风险操作 | `display.confirm.show` |
| 向用户追问信息 | `input.text.request` / `input.choice.request` |
| 展示长任务进度 | `agent.progress.update` / `agent.step.update` |
| 宠物说短句 | `pet.say` |
| 改变宠物状态 | `pet.emotion.set` |

---

## 6. 判断是否值得打扰的建议规则

第一版可以不用复杂模型，先用规则 + 简单 AI 判断。

### 6.1 建议弹出

- 私聊里明确让用户做事。
- 群聊中 @ 用户且包含问题/请求。
- 消息包含截止时间、确认、审批、发布、故障、风险等关键词。
- AI 判断用户是 action owner。
- 后端已经生成了草稿或结果，等待用户确认。
- 长任务完成，需要用户查看或继续操作。

### 6.2 不建议弹出

- 普通群聊闲聊。
- 普通系统通知。
- 只是在同步信息，没有用户动作。
- 已经由飞书原生通知很好覆盖的场景。
- 同一话题短时间内重复消息。

---

## 7. 对端可以产出的“高价值事件”

### 7.1 `actionable_message`

AI 判断一条消息需要用户行动。

```json
{
  "type": "display.message.show",
  "payload": {
    "title": "产品群需要你确认",
    "subtitle": "张三 · 产品群",
    "message": {
      "type": "text",
      "content": {
        "plain_text": "下午四点能给个方案吗？"
      }
    },
    "assistant_message": "我建议回复：可以，我四点前发初版。",
    "actions": [
      { "id": "send_suggestion", "label": "发送建议", "style": "primary", "intent": "confirm" },
      { "id": "edit", "label": "修改", "style": "normal", "intent": "edit" },
      { "id": "ignore", "label": "忽略", "style": "quiet", "intent": "cancel" }
    ],
    "metadata": {
      "provider": "lark",
      "chat_id": "oc_xxx",
      "message_id": "om_xxx"
    }
  }
}
```

---

### 7.2 `daily_briefing`

AI 汇总今日待处理。

```json
{
  "type": "display.card.show",
  "payload": {
    "title": "今天需要你处理的 3 件事",
    "subtitle": "AI 从飞书消息、任务、会议中整理",
    "elements": [
      { "tag": "markdown", "content": "**1. 产品群回复**：张三在等方案确认" },
      { "tag": "hr" },
      { "tag": "markdown", "content": "**2. 会议待办**：补充接口说明" },
      { "tag": "markdown", "content": "**3. 审批风险**：发布审批需要你确认" }
    ],
    "actions": [
      { "id": "view_detail", "label": "查看详情", "style": "primary" },
      { "id": "create_tasks", "label": "生成任务", "style": "normal" }
    ]
  }
}
```

---

### 7.3 `needs_context`

AI 缺信息，需要用户补充。

```json
{
  "type": "input.choice.request",
  "payload": {
    "choice_id": "target-chat-001",
    "title": "你想发给哪个群？",
    "description": "我找到几个可能相关的飞书群。",
    "options": [
      { "id": "product", "label": "产品群" },
      { "id": "dev", "label": "研发群" },
      { "id": "project", "label": "项目群" }
    ],
    "metadata": {
      "task_id": "draft-send-001"
    }
  }
}
```

---

## 8. 产品路线图

### 第一阶段：AI 关键消息助手

目标：不是消息通知，而是行动提醒。

能力：

- AI 判断消息是否需要用户处理。
- 展示关键消息卡片。
- 生成回复草稿。
- 用户确认/修改/忽略。
- 回传 action 给后端。

验收：

> 群里 100 条消息，miniPet 只出现 1 次，而且这 1 次确实值得处理。

---

### 第二阶段：桌面投喂入口

能力：

- 拖文本/文件/截图/链接给宠物。
- 宠物询问处理方式。
- 后端执行总结、转任务、起草回复、发送到飞书。

验收：

> 用户不需要打开飞书，也能把桌面上的信息交给 AI 处理。

---

### 第三阶段：AI 长任务陪跑

能力：

- 任务进度。
- 步骤流。
- 完成后收进待办托盘。
- 用户随时取消/继续。

验收：

> 用户能看见 AI 在干什么，而不是等一个黑盒结果。

---

### 第四阶段：低打扰办公搭子

能力：

- 专注模式。
- 只打扰重要事项。
- 宠物状态表达用户/AI 工作状态。
- 每日/每周主动汇总。

验收：

> miniPet 从“通知工具”变成“桌面 AI 办公搭子”。

---

## 9. 最重要的一条原则

miniPet 每次出现，都应该回答一个问题：

> 这件事为什么值得现在打扰用户？

如果回答不了，就不要弹。

普通消息让飞书通知处理。

miniPet 只处理：

- AI 判断后的关键事项。
- 需要用户决策的时刻。
- 需要补充上下文的时刻。
- 需要确认风险操作的时刻。
- 用户主动投喂和主动命令。

这才是桌面宠物比普通通知更有价值的地方。
