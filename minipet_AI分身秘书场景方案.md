# MiniPet AI 分身秘书场景方案

这是一份偏产品叙事和 PPT 的场景文档，用来说明 MiniPet 为什么不应该只是“飞书通知换皮”，而应该成为桌面上的 AI 分身秘书。

---

## 1. 一句话愿景

> MiniPet 不是提醒每一条消息，而是在关键决策时刻出现，把复杂工作压缩成几个用户需要确认的决定。

或者更产品化一点：

> MiniPet 是有身体的 AI 办公代理。它不把消息搬到桌面，它把工作压缩成决策、确认和行动。

---

## 2. 最科幻的核心场景：桌面上的 AI 分身秘书

你正在电脑前写代码、做方案、开会或处理文档。

MiniPet 不只是一个宠物，也不是一个通知弹窗。它像一个有身体的 AI 秘书，在后台替你守门、合并上下文、准备草稿，只在需要你做判断的时候出现。

### 一句话画面

> 你在专注写代码，桌宠突然从屏幕边探出头，手里举着一张小卡片：
>
> “我帮你挡了 37 条飞书消息，只剩 2 件事需要你决定。第一件我已经写好回复了，要发吗？”

这才像 AI 助理。

---

## 3. 完整体验脚本

### 3.1 它先替你守门

上午 10:00，用户开启专注模式。

飞书里继续有消息：

- 产品群在讨论接口细节。
- 项目群有人同步进度。
- 老板问方案什么时候能出。
- 测试群报了一个疑似线上问题。
- 日历里 11 点有会。

普通通知会怎样？

```text
叮叮叮叮叮。
```

MiniPet 应该怎样？

它不弹普通消息。

它在后台让后端判断：

```text
哪些是噪音？
哪些只是同步？
哪些可以等？
哪些真的需要用户现在拍板？
哪些可以由 AI 先处理？
```

桌面上只保留一个低打扰状态：

```text
小宠物戴着耳机坐在屏幕角落。
状态：专注守门中。
```

---

### 3.2 它主动合并上下文，而不是转发消息

10:27，MiniPet 轻轻敲屏幕。

不是因为“有新消息”。

而是因为它发现：

```text
产品群讨论了 18 条消息。
项目文档里有一处接口说明没更新。
测试群刚好提到同一个接口。
老板问你四点能不能给方案。
```

这些单条看都只是消息。合起来才是一个事件：

> 今天四点前，接口方案需要你拍板。

MiniPet 出现：

```text
我把刚才 3 个群里的讨论合并看了一下。
现在真正需要你决定的是这个：

接口鉴权方案今天是否按 A 版推进？

我建议：先按 A 版推进，B 版作为后续优化。
原因：
1. 测试反馈的问题不影响 A 版上线
2. 文档只需要补一个字段说明
3. 老板问的是时间，不是方案细节

要我帮你回复产品群和老板吗？
```

按钮：

```text
[按建议回复]
[先给我看证据]
[改成保守一点]
[忽略]
```

这已经不是通知，而是：

> 上下文合并 + 决策压缩 + 桌面确认入口。

---

### 3.3 用户点“先给我看证据”

MiniPet 展开一张小卡片：

```text
证据链：

产品群 · 张三 10:12
“如果鉴权字段今天能定，前端下午就能接。”

测试群 · 李四 10:18
“当前问题只影响异常文案，不阻塞联调。”

接口文档 · 10:21
缺少字段：auth_scope

老板 · 10:25
“下午四点能给个方案吗？”
```

下面有两个操作：

```text
[打开原消息]
[相信你，继续]
```

注意：MiniPet 没有变成飞书客户端。它只是给出关键证据链，让用户做判断。

---

### 3.4 用户点“按建议回复”

MiniPet 不直接发送。

它变成认真表情，弹出确认：

```text
我准备做 3 件事：

1. 回复老板：
“可以，四点前给初版方案，目前按 A 版推进。”

2. 回复产品群：
“鉴权方案先按 A 版推进，B 版后续优化。前端可以先接。”

3. 在接口文档待办里补一项：
“补充 auth_scope 字段说明。”

是否执行？
```

按钮：

```text
[执行]
[逐条修改]
[取消]
```

桌宠在这里的价值是：

> AI 可以做事，但重要动作必须在桌面上被用户看见、确认、授权。

---

### 3.5 用户点“逐条修改”

MiniPet 不打开复杂后台，而是在桌面旁边进行微交互编排。

先改给老板的回复：

```text
可以，四点前给初版方案，目前按 A 版推进。
```

用户改成：

```text
可以，我四点前给初版方案，当前倾向先按 A 版推进，风险点我会一起标出来。
```

MiniPet 接着问：

```text
产品群回复要更正式还是更口语？
```

按钮：

```text
[正式]
[口语]
[保持现在]
```

这是 AI 助理式的桌面微交互，而不是一个消息弹窗。

---

### 3.6 执行时，宠物陪跑

用户确认后，MiniPet 切换成工作状态：

```text
正在替你处理：
✓ 回复老板
● 回复产品群
○ 创建文档待办
```

如果某一步失败：

```text
产品群回复失败：缺少发送权限。
我可以把内容复制给你，或者打开飞书让你手动发送。
```

按钮：

```text
[复制内容]
[打开飞书]
[重试]
```

这比普通系统报错更像一个助理。

---

### 3.7 完成后，它给结果收口

```text
处理好了。

已完成：
✓ 回复老板
✓ 回复产品群
✓ 创建文档待办

我还发现一个后续风险：
测试群提到的异常文案，如果今天不改，明天验收可能会被提出来。

要不要我顺手建一个低优先级任务？
```

按钮：

```text
[创建]
[明天提醒]
[不用]
```

这才是桌面 AI 助理：不是一次性通知，而是把工作闭环做完。

---

## 4. 为什么这个场景科幻

它体现了普通通知做不到的 5 个能力。

### 4.1 从“消息”升级为“事件”

普通通知：

```text
张三发来一条消息。
```

MiniPet：

```text
三个群、一个文档、一个老板问题，合并成一个待决策事件。
```

---

### 4.2 从“提醒你看”升级为“帮你判断”

普通通知：

```text
你自己判断重不重要。
```

MiniPet：

```text
我判断后，只把关键决策点拿给你。
```

---

### 4.3 从“打开应用处理”升级为“桌面确认即处理”

普通通知：

```text
点击 → 打开飞书 → 找消息 → 想回复 → 输入 → 发送
```

MiniPet：

```text
确认 → 后端执行 → 宠物反馈结果
```

---

### 4.4 从“单点消息”升级为“上下文证据链”

普通通知只给原文。

MiniPet 给：

```text
结论 + 原因 + 证据 + 可执行动作
```

---

### 4.5 从“工具”升级为“角色”

因为它有：

- 状态。
- 动作。
- 情绪。
- 语音。
- 追问。
- 陪跑。
- 收口。

它不是一个 API 面板，而是一个桌面 AI 分身。

---

## 5. PPT 叙事版本

### 第 1 页：标题

# MiniPet：你的桌面 AI 分身秘书

副标题：

> 不再提醒每一条消息，只在关键决策时出现。

---

### 第 2 页：问题

# 办公通知不是太少，而是太多

- 飞书消息不断打断专注。
- 重要事项淹没在群聊里。
- AI 后端做了事，但用户感知不到。
- 高风险操作缺少自然确认入口。
- 用户仍然要在多个应用间切换。

---

### 第 3 页：洞察

# 用户不需要更多通知，用户需要更少但更聪明的打扰

普通通知回答：

> 谁说了什么？

MiniPet 回答：

> 你现在要不要做什么？

---

### 第 4 页：产品定位

# MiniPet 是 AI 办公能力的桌面身体

- AI 的脸：展示状态和情绪。
- AI 的嘴：主动解释和追问。
- AI 的手：承载确认和执行反馈。
- AI 的入口：接收用户投喂和命令。
- AI 的护栏：高风险操作本地确认。

---

### 第 5 页：核心体验

# 从消息流到决策流

```text
飞书消息 / 文档 / 会议 / 任务
        ↓
AI 合并上下文
        ↓
提炼关键决策
        ↓
MiniPet 桌面确认
        ↓
后端执行
        ↓
MiniPet 收口反馈
```

---

### 第 6 页：示例场景

# 它帮你挡了 37 条消息，只留下 2 个决定

桌宠出现：

> “我帮你看了刚才的讨论，真正需要你决定的是：接口方案是否按 A 版推进？”

按钮：

```text
[按建议回复]
[看证据]
[改保守点]
```

---

### 第 7 页：差异化

# 为什么不是飞书通知？

| 飞书通知 | MiniPet |
| --- | --- |
| 消息级 | 事件级 |
| 原文转发 | 上下文合并 |
| 让用户判断 | AI 先判断 |
| 打开应用处理 | 桌面确认处理 |
| 静态通知 | 有状态的 AI 角色 |
| 通知结束即结束 | 任务陪跑和结果收口 |

---

### 第 8 页：技术协议

# 对端只需要发交互意图，不需要控制 UI 细节

```json
{
  "type": "interaction.present",
  "payload": {
    "kind": "decision_required",
    "title": "接口方案需要你确认",
    "content": "AI 判断当前只需要你拍板 A/B 方案。",
    "actions": [
      { "id": "approve_a", "label": "按 A 版推进" },
      { "id": "show_evidence", "label": "看证据" }
    ]
  }
}
```

---

### 第 9 页：未来感

# AI 不再藏在聊天框里，而是站在桌面上替你跑腿

- 它知道你在忙，所以不乱叫。
- 它知道事情轻重，所以只挑关键的说。
- 它知道自己能做什么，所以先准备草稿。
- 它知道什么不能擅自做，所以请你确认。
- 它做完后会回来告诉你结果。

---

## 6. 对协议设计的启发

这个场景要求后端不要发送“普通消息通知”，而是发送“交互意图”。

推荐事件类型：

| 意图 | 协议 |
| --- | --- |
| 需要用户决策 | `interaction.present` |
| 展示证据链 | `display.card.show` |
| 展示关键来源消息 | `display.message.show` |
| 高风险执行确认 | `display.confirm.show` |
| 缺上下文追问 | `input.text.request` / `input.choice.request` |
| 执行进度陪跑 | `agent.progress.update` / `agent.step.update` |
| 完成收口 | `agent.task.done` + `display.card.show` |

后端不要问：

> 这条飞书消息怎么弹？

后端应该问：

> 这件事是否构成一个需要用户决策或确认的桌面交互？

---

## 7. 最终一句话

> MiniPet 的终极形态不是“桌面通知宠物”，而是“有身体的 AI 办公代理”。
>
> 它不把消息搬到桌面，它把工作压缩成几个需要你确认的决定。
