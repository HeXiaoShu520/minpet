# MiniPet 未来展望

本文收纳尚未进入当前实现的产品与协议方向。当前可接入能力以 [MiniPet 当前协议设计](MiniPet%20当前协议设计.md) 为准，近期功能设计以 [MiniPet 完整功能设计](MiniPet%20完整功能设计.md) 为准。本文示例均是提案，不可直接假设现有客户端已支持。

## 1. 终极愿景

> MiniPet 是有身体的 AI 办公代理：它不把消息搬到桌面，而是把复杂工作压缩成少数需要用户确认的决定。

未来 MiniPet 应成为 AI 的桌面身体：

- 脸：用角色、状态和情绪让后台能力可感知；
- 嘴：解释判断、主动追问、用语音自然反馈；
- 手：承载确认、取消、修改和恢复；
- 入口：接收命令、图片、文件、链接和显式授权上下文；
- 护栏：在高风险操作前提供本地可见确认；
- 记忆点：把长任务、待处理事项和结果收口留在可回访空间。

科幻感不来自更多动画，而来自五个能力：把多条消息合并为事件、先替用户判断、给出可验证证据、在桌面完成授权、执行后回来收口。

## 2. 演进原则

1. **保持核心信封稳定**：优先延续 `session`、`user`、`surface`，避免为每种业务新增顶层事件。
2. **语义优先于控件**：后端表达任务和交互意图，客户端决定合适的桌面呈现。
3. **渐进增强**：能力必须通过 hello/ready 协商；不支持时退回文本、基础卡片或聊天窗口。
4. **不把 MiniPet 变成浏览器**：复杂工作台应克制，业务详情仍可打开原应用。
5. **授权先于上下文**：任何新增采集能力都必须显式、可撤销、可审计。
6. **高风险动作可见**：自动化程度提高不能绕过用户确认和审计。
7. **兼容层有期限**：旧字段可读取，但新能力只进入正式版本化规范。

## 3. 更丰富的 Surface

当前卡片可表达文本、轻量 Markdown、输入、单选、多选和按钮。未来可在 `surface.*` 生命周期下扩展语义化 surface，而不是新增一组 show/update/close 事件。

候选类型：

| kind | 用途 | 基础降级 |
| --- | --- | --- |
| `timeline` | 长任务步骤与等待节点 | Markdown 步骤列表 |
| `feed` | 每日摘要、关键事项流 | 多个 Markdown 段落 |
| `dashboard` | 宠物展开工作台 | 打开聊天或独立窗口 |
| `inbox` | 待处理事项托盘 | 摘要卡片 |
| `badge` | 低打扰数量或状态 | 不展示或 Toast |
| `form` | 多字段轻表单 | 现有 controls |
| `media` | 图片、音频、文件预览 | 文本链接与说明 |
| `evidence` | 结论对应的来源与证据链 | Markdown 引用列表 |

这些 kind 在能力协商完成前都只是设计名称。

### 3.1 Timeline

```json
{
  "type": "surface.show",
  "payload": {
    "surface_id": "schedule-meeting-001",
    "kind": "timeline",
    "steps": [
      {"id": "parse", "title": "理解需求", "status": "done"},
      {"id": "freebusy", "title": "查询空闲时间", "status": "running"},
      {"id": "confirm", "title": "等待选择时间", "status": "pending"}
    ],
    "actions": [
      {"id": "cancel", "label": "取消", "style": "quiet"}
    ]
  }
}
```

Timeline 应支持步骤更新、部分失败和等待用户，不应要求后端反复拼 Markdown。客户端不支持时，后端应提供 `content` 作为降级文本。

### 3.2 Feed 与证据链

Feed 适合“今天真正需要处理什么”，证据链适合“为什么建议这样做”。未来结构应区分结论、来源和动作：

```json
{
  "type": "surface.show",
  "payload": {
    "surface_id": "morning-feed-001",
    "kind": "feed",
    "content": "今天有 3 件事需要处理。",
    "items": [
      {
        "id": "item-1",
        "title": "接口方案需要确认",
        "summary": "产品与测试信息合并后，建议先按 A 版推进。",
        "priority": "important",
        "evidence_refs": ["msg-1", "doc-1"]
      }
    ]
  }
}
```

证据来源应能打开原消息或文档，但 MiniPet 只展示关键片段，不承担完整内容浏览。

### 3.3 Dashboard、Inbox 与 Badge

Dashboard 是宠物展开后的轻量工作台，可汇总正在运行、等待确认和最近完成的任务。Inbox 保存暂不值得弹出的事项；Badge 只提示数量或优先级。

```json
{
  "type": "surface.update",
  "payload": {
    "surface_id": "pet-badge",
    "kind": "badge",
    "value": 3,
    "level": "important",
    "tooltip": "有 3 个事项等待确认"
  }
}
```

Badge 必须克制，不能退化成另一个红点系统。只有经过聚合和判断的待处理事项才应计数。

## 4. Agent 状态与任务图

代码目前定义了 `agent.state` 名称，但未声明能力、也未实现专门处理。未来若正式引入，应先定义状态模型和与 surface 的关系。

建议原则：

- `agent.state` 表达代理全局或任务运行状态，不直接决定 UI；
- 需要用户看到的状态投射为 surface；
- 任务图有稳定 task/step ID；
- 状态更新必须可乱序防护和幂等；
- 失败、取消、等待用户是一级状态。

提案：

```json
{
  "version": "2.0-draft",
  "type": "agent.state",
  "payload": {
    "state": "working",
    "task": {
      "id": "briefing-001",
      "title": "整理今日待处理",
      "steps": [
        {"id": "messages", "title": "检索消息", "status": "done"},
        {"id": "tasks", "title": "整理任务", "status": "running"}
      ]
    }
  }
}
```

在进入正式协议前，需要明确：多任务并行、任务取消、进度百分比可信度、重连恢复、历史保留和隐私展示。

## 5. 上下文授权协议

未来外置后端可能希望获取当前屏幕、选中文本、窗口、剪贴板或本地文件。不能靠隐式采集，应设计显式授权流程：

```text
context.request → MiniPet 展示将读取什么 → 用户允许/拒绝
context.response → 只返回本次授权内容
```

候选请求：

```json
{
  "type": "context.request",
  "request_id": "ctx-001",
  "payload": {
    "items": ["active_window_title", "selected_text", "screenshot"],
    "reason": "分析当前页面中的报错",
    "scope": "once"
  }
}
```

必须满足：

- 用户看得到请求者、用途和具体字段；
- 默认一次性授权，不自动扩大范围；
- 敏感内容在发送前可预览或遮挡；
- 拒绝后后端收到明确结果并可降级；
- 授权可撤销，历史可审计；
- 屏幕录制、持续监听与一次截图分开授权；
- 文件路径与文件内容是不同权限。

## 6. 能力协商增强

当前只交换事件名。未来需要协商 surface、格式、附件、限制和安全能力：

```json
{
  "protocol": "minipet.v2",
  "surface_kinds": ["card", "timeline", "feed", "inbox"],
  "features": {
    "surface_update": true,
    "surface_close": true,
    "markdown": "safe_subset",
    "context_request": true,
    "file_attachment": false,
    "action_structured_response": true
  },
  "limits": {
    "max_actions": 4,
    "max_content_chars": 4000,
    "max_attachment_bytes": 5242880
  }
}
```

协商应区分“能接收事件”和“能完整渲染字段”。后端必须提供降级内容，不应仅凭版本号推测能力。

## 7. 结构化动作回传

当前卡片操作被拼成 `user.input.text`，简单但会丢失 surface、action 和 values 的结构关联。未来可在保持自然语言输入的同时增加结构化字段：

```json
{
  "type": "user.input",
  "payload": {
    "text": "确认发送",
    "mode": "surface_action",
    "surface_id": "draft-001",
    "turn_id": "turn-001",
    "action": {"id": "send", "intent": "confirm"},
    "values": {"tone": "formal"}
  }
}
```

这比新增 `interaction.action` 顶层事件更符合“小核心”原则，也便于审计、幂等和防止模型误解。引入前必须定义：按钮重复点击、卡片过期、服务端幂等 key、敏感值脱敏和本地 action 保留范围。

## 8. 附件与媒体

未来附件协议应覆盖图片之外的文件、链接和媒体，同时避免把任意本地路径直接暴露给远程后端。

需要定义：

- 内联 base64 与临时上传引用；
- MIME、大小、哈希和文件名；
- 是否包含真实本地路径；
- 远程后端与 localhost 后端的不同策略；
- 上传进度、取消和过期；
- 恶意文件与内容扫描；
- 图片压缩、OCR 和用户可见预览。

大文件不应塞进 WebSocket JSON。可采用本地临时对象服务或经用户确认的分块上传，但必须有 token、生命周期和来源限制。

## 9. 权限、信任与审计

未来需要从“用户填写一个 WebSocket 地址”演进到可管理的信任模型：

- 后端白名单和首次连接确认；
- localhost 与远程地址的明显区分；
- TLS、token、设备绑定或短期配对码；
- 后端身份和声明能力展示；
- 每类上下文与业务动作的权限开关；
- 高风险动作二次确认；
- 本地审计日志：谁请求、用户如何确认、执行结果；
- 可撤销授权与一键断开；
- 对文件、截图和剪贴板的醒目标识。

审计日志应记录语义和结果，不默认保存完整敏感正文。后端声称“已执行”与业务系统真实结果需要可核验。

## 10. 多任务、多设备与多宠物

若未来支持多个后端、设备或宠物，需要引入明确归属：

- `session_id`
- `device_id`
- `pet_id`
- `user_id`
- `task_id` / `turn_id`
- surface 所有者与可见设备
- 同步版本和冲突策略

关键问题包括：同一确认能否在两台设备点击、哪个宠物代表哪个后端、任务是否跨设备继续、离线操作如何处理、个人与企业身份如何隔离。不能只添加 ID 字段而不定义一致性规则。

## 11. AI 分身秘书的长期场景

### 11.1 跨来源决策压缩

后端把三个群、一份文档和一个日程合并为一个“今天 16:00 前需要定方案”的事件。MiniPet 给结论、建议与证据链，用户确认后，后端分别回复消息并创建待办。

### 11.2 可解释的专注守门

宠物处于“守门”状态，普通信息进入 Inbox。真正紧急的事项出现时，卡片说明为何现在打扰；用户可反馈“以后这类不要提醒”或“这个项目总是提醒”，形成透明可控的个性化策略。

### 11.3 主动但有边界的代理

AI 可先完成低风险准备工作：检索、汇总、起草、排方案；涉及发送、删除、付款、发布和权限变更时停在确认节点。用户不是逐步遥控工具，而是在关键节点监督代理。

### 11.4 任务陪跑与结果收口

宠物不仅说“开始了”，还持续表达任务阶段、等待项、失败恢复和最终结果。完成后把后续风险放入 Inbox，而不是立即用另一个弹窗打扰。

## 12. 建议演进路线

### V1 稳定期

- 固化当前信封、session、user.input 和 surface 行为；
- 修正文档与实现偏差；
- 增加协议级自动化测试；
- 新后端停止使用旧 message/bubble/action 事件；
- 明确累计流文本、终态和关联字段。

### V1.x 渐进增强

- 在 capabilities 中声明具体渲染和限制；
- 为 `user.input` 增加可选结构化 action 字段；
- 增加安全的附件元数据与大小限制；
- 为不支持的新字段提供文本降级；
- 保持旧 V1 后端无需立即迁移。

### V2 草案

- 版本化定义 timeline/feed/inbox/evidence；
- 正式定义 agent task graph；
- 引入 context 授权协议；
- 定义认证、幂等、重连恢复和审计；
- 提供 schema、示例实现和兼容性测试套件。

### 多端阶段

- 会话、设备、宠物和任务归属；
- 跨设备确认冲突；
- 企业权限和身份隔离；
- 本地优先数据与同步策略。

## 13. 旧协议退出策略

历史 `message`、`bubble`、`action` 以及早期 `display.*`、`input.*`、`interaction.*` 设想不应继续成为新入口。

建议：

1. 当前客户端只保留未知事件 Toast 等有限兼容，不宣传其为完整协议；
2. 对已知旧后端在适配层转换为 V1 `surface.*`；
3. 通过日志或开发工具标记 deprecated 输入；
4. V2 发布时公布兼容期限和迁移表；
5. 删除兼容前先确认真实使用量，而不是长期维护双协议语义。

## 14. 不应做的未来

- 不把所有飞书通知复制到宠物旁边；
- 不为每个业务系统设计一套顶层事件；
- 不让远程后端任意控制宠物、窗口和本机操作；
- 不默认持续截屏或监听剪贴板；
- 不用复杂动态 UI schema 复制完整 Web 应用；
- 不以“主动弹出次数”衡量智能程度；
- 不在没有幂等和审计时开放一键高风险执行。

未来的 MiniPet 应更主动，但也更克制；能力更强，但用户始终知道它为何出现、拿到了什么、准备做什么以及最终做成了什么。
