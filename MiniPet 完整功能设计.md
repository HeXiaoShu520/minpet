# MiniPet 完整功能设计

本文说明 MiniPet 作为桌面 AI 宠物的产品定位、当前体验原则、核心场景和落地优先级。协议的当前可用字段与行为以 [MiniPet 当前协议设计](MiniPet%20当前协议设计.md) 为准；本文讨论“应该做成什么体验”，不把尚未实现的设想写成现状。

## 1. 产品定位

MiniPet 不是通知中心，也不是缩小版飞书或聊天客户端。它是 AI 在桌面上的可见交互层：

> 让 AI 在桌面上有一个可见、可互动、可投喂、可追问、可确认的身体。

普通通知回答“发生了什么”；MiniPet 应优先回答：

> 你现在要不要做什么？

MiniPet 的价值不在于搬运更多消息，而在于把分散信息压缩成少数值得用户决定、补充或确认的交互。

## 2. 职责边界

MiniPet 应负责：

- 接受用户主动输入：文字、语音、图片、桌面拖拽；
- 展示 AI 的回复、建议、追问、确认和任务状态；
- 在重要动作前提供清晰的桌面确认；
- 用宠物、卡片和声音表达“正在听、思考、等待、完成、失败”；
- 让用户不必切换到复杂后台即可完成一次轻量决策；
- 在后端完成工作后给出结果收口。

外置后端应负责：

- 获取飞书、邮件、日历、文档、任务等业务上下文；
- 判断事项是否值得打扰；
- 合并来源、生成草稿、解释理由和证据；
- 调用真实业务 API；
- 保存业务任务状态、权限与审计信息。

MiniPet 不应：

- 转发每一条普通消息、日程或审批红点；
- 承担完整会话列表、已读未读和文件管理；
- 在没有用户知情的情况下采集敏感上下文；
- 由展示层直接执行高风险业务操作；
- 让后端操纵宠物动作来制造不可控打扰。

## 3. 核心体验原则

### 3.1 少打扰，但每次出现都有价值

每次主动出现前都应回答：“为什么值得现在打扰用户？”

适合出现：

- 用户明确被要求做事或拍板；
- 有截止时间、发布、故障、权限、付款等风险；
- AI 已准备好草稿或结果，只差用户确认；
- AI 缺少一个关键上下文，无法继续；
- 长任务达到需要用户介入或已完成的节点；
- 用户主动发起命令或投喂内容。

不适合出现：

- 普通群聊、系统通知和信息同步；
- 已被原生应用很好覆盖的提醒；
- 同一话题的重复消息；
- 没有建议、没有动作、也不紧急的摘要；
- 仅用于表现“AI 很忙”的频繁动画。

第一阶段可采用规则与模型判断组合：是否 @ 用户、是否包含请求和截止时间、用户是否 action owner、风险关键词、是否已有可执行草稿、同话题去重和冷却。

### 3.2 从消息流转为决策流

推荐链路：

```text
消息 / 文档 / 会议 / 任务
        ↓
后端补全并合并上下文
        ↓
判断是否值得打扰
        ↓
提炼一个决策、追问或确认
        ↓
MiniPet 桌面卡片
        ↓
用户选择或补充
        ↓
后端执行
        ↓
MiniPet 汇报结果与后续风险
```

后端不应问“这条飞书消息怎么弹”，而应问“这组信息是否构成一个需要用户决策的桌面交互”。

### 3.3 先给结论，再允许查看证据

主动卡片第一屏应短而明确：

1. 发生了什么；
2. 为什么与用户有关；
3. 建议用户做什么；
4. 可选动作是什么。

证据链、原始消息和详细原因应按需展开，而不是一开始把完整聊天记录塞进卡片。这样既降低打扰，也保留用户判断权。

### 3.4 高风险动作必须确认

发送消息、发邮件、创建或删除任务、修改文档、安排会议、批量操作等，应在执行前明确展示：

- 将执行什么；
- 对谁或对哪些对象执行；
- 实际内容；
- 是否可撤销；
- 哪些步骤可能失败。

当前协议可用 `elements + controls + actions` 完成确认。按钮只把用户选择交给后端，真正执行仍由后端负责。

### 3.5 长任务必须可感知、可中断、可收口

长任务不能只显示一个无限“处理中”。当前实现可用同一 `surface_id` 的累计文本和 `status` 更新：

```text
正在整理会议：
✓ 读取纪要
✓ 提取待办
● 匹配负责人
○ 等待确认
```

用户应能中断当前回复；失败时给出可行动的恢复选项，如重试、复制结果、打开原应用。完成后要说明已完成、未完成和新发现的风险，而不是卡片消失即结束。

### 3.6 宠物是角色，不是业务状态机

宠物可以通过本地体验表达：空闲、聆听、思考、说话、等待和失败。但业务语义应由卡片文字承担，不能只靠动作传达。当前 V1 不接受后端宠物动作指令，这是有意的安全与体验边界。

## 4. 当前交互载体

### 4.1 快速文字与聊天窗口

- 双击桌宠可快速提问；
- 独立聊天窗口适合连续对话和查看后端分类历史；
- 外置 custom 后端通过 `turn_id` / `surface_id` 将 surface 回复关联到请求；
- 简短决策优先留在桌面卡片，复杂长聊再打开聊天窗口。

### 4.2 回复卡片

卡片由三类内容按需组成：

```text
Card
├─ elements：结论、证据、步骤、说明
├─ controls：文本、单选、多选、自定义补充
└─ actions：确认、修改、查看、取消等动作
```

设计建议：

- 正文优先用 `content` 或一个主要 Markdown element；
- 一张卡只承载一个主要决策；
- 按钮不超过 4 个，主按钮最多一个；
- 选项短而互斥，复杂编辑转入文本框或聊天窗口；
- 流式回复使用同一 `surface_id` 原位更新；
- 完成和失败都给明确 `status` 与足够阅读时间。

### 4.3 语音

语音球适合即时问答和轻量操作，不适合读出长证据链。外置 surface 正文可被 TTS 播报，因此后端应：

- 第一段尽快给出自然语言结论；
- 避免播报“处理中”等机械占位；
- 让累计文本保持稳定，避免大范围改写造成重复播报；
- 长列表先口头总结，再在卡片中展示详情。

### 4.4 桌面投喂

用户可把图片或其他桌面内容拖给宠物，再选择“总结、生成待办、起草回复、发到飞书、询问”等意图。体验重点是先问“怎么处理”，而不是默认执行。

当前实现中图片可作为 base64 附件交给后端；普通文件可能只提供路径与元信息。产品文案必须真实反映此限制，避免让用户误以为文件内容已上传。

## 5. 核心场景

### 5.1 关键消息与自动草稿

后端看到大量消息后，只在真正需要用户行动时出现：

```text
产品群有一件事需要你确认：
张三希望今天 16:00 前拿到接口方案。

建议回复：可以，我四点前发初版，有变动提前同步。

[确认发送] [修改语气] [看上下文] [忽略]
```

“修改语气”应触发下一轮 `user.input`，后端生成新草稿；“确认发送”仍应在必要时展示最终内容并执行。

### 5.2 缺少上下文时追问

```text
你说的“发给客户”，我找到两个可能的群。

( ) 甲方项目群
( ) 售前沟通群
( ) 其他：________

[继续] [取消]
```

当前卡片单选与自定义输入足以实现。追问应只索取完成任务所需的最小信息。

### 5.3 会议后收口

```text
刚才会议中有 3 个与你有关的待办：
1. 补接口文档，今天 18:00 前
2. 确认排期，明天上午
3. 给客户发版本说明

[全部创建] [逐条确认] [复制] [忽略]
```

后端执行后应更新同一张卡片，列出每项成功或失败，并提供失败项恢复动作。

### 5.4 截图和图片投喂

用户拖入截图：

```text
这张图想怎么处理？
[识别文字] [总结问题] [生成任务] [询问 AI]
```

如果用户选择“发给某人”，下一步追问对象，再展示最终发送确认。流程应逐步收窄，不在第一张卡塞入所有字段。

### 5.5 AI 长任务陪跑

```text
正在整理上周 MiniPet 协议讨论：
✓ 搜索消息
✓ 搜索文档
● 汇总结论
○ 生成摘要

[取消]
```

当前协议可通过累计 Markdown 文本表达步骤。完成后更新为结果摘要，并提供“复制”“打开聊天”“继续处理”等动作。

### 5.6 专注模式与智能守门

目标体验：普通消息保持安静，只有紧急且确需当前决定的事项轻量出现。

验收不应是“拦截了多少通知”，而应是：

- 重要事项漏报率可接受；
- 主动出现次数明显少于原生通知；
- 用户认为大多数出现都值得处理；
- 相同话题不会反复打扰。

专注模式需要后端优先级判断、去重与批量摘要能力；当前 MiniPet 客户端尚没有完整的专注守门产品模块，应作为后续功能开发，而不是协议适配器自行假装实现。

## 6. 完整“AI 分身秘书”体验脚本

用户正在专注工作。后端合并了产品群、测试群、接口文档和老板私聊，判断真正需要决定的是“鉴权方案今天是否按 A 版推进”。MiniPet 不转发 37 条消息，而是出现：

```text
我帮你看了刚才的讨论，真正需要你决定的是：
鉴权方案今天是否先按 A 版推进？

建议先按 A 版推进，B 版后续优化。
[按建议处理] [看证据] [改保守一点] [忽略]
```

用户选择“看证据”，后端更新卡片：

```text
证据：
- 产品群：前端今天需要确定字段
- 测试群：当前问题不阻塞联调
- 接口文档：缺少 auth_scope 说明
- 老板：希望 16:00 前拿到方案

[相信建议，继续] [取消]
```

用户继续后，卡片清楚列出拟执行动作：

```text
准备执行：
1. 回复老板：四点前给初版，当前倾向 A 版
2. 回复产品群：先按 A 版推进
3. 创建“补充 auth_scope 字段说明”待办

[执行] [逐条修改] [取消]
```

执行中以同一 surface 展示步骤；完成后列出结果和一个后续风险。这个脚本体现 MiniPet 的核心差异：上下文合并、决策压缩、桌面授权、任务陪跑、结果收口。

## 7. 后端交互设计准则

### 7.1 输出交互意图，而非原始通知

虽然后端最终通过当前 `surface.*` 发送 UI 数据，但生成层应先形成内部交互意图，例如：

- `decision_required`：需要用户拍板；
- `draft_confirmation`：草稿待确认；
- `needs_context`：缺少信息；
- `task_progress`：任务执行中；
- `task_result`：完成或部分失败；
- `important_briefing`：低频关键摘要。

再把意图映射到当前卡片 schema。不要把历史设想中的 `display.message.show`、`input.choice.request` 等直接发给当前客户端；它们不是现行协议。

### 7.2 文案结构

主动卡片推荐顺序：

1. 一句结论；
2. 一句关联原因或建议；
3. 必要的精简证据；
4. 2～4 个动作。

避免：模糊的“有事项待处理”、没有对象的“是否执行”、过长背景、四个同权重按钮、技术错误堆栈。

### 7.3 失败与降级

失败卡片必须说明：

- 哪一步失败；
- 已完成的步骤是否保留；
- 用户现在能做什么；
- 是否可安全重试。

推荐降级动作：复制内容、重试、打开原应用、保留草稿、取消。不要静默重试高风险操作。

## 8. 功能路线与验收

### 阶段一：可靠的关键交互闭环

能力：

- 关键消息卡片；
- 回复草稿；
- 追问和选择；
- 高风险确认；
- surface 流式更新与失败恢复。

验收：100 条群消息中只出现极少数真正需要处理的事项；每个确认动作都有明确对象和内容；协议关联与聊天记录正确。

### 阶段二：桌面投喂入口

能力：

- 图片、截图和链接投喂；
- 处理意图选择；
- 总结、待办、草稿等后端动作；
- 附件与权限提示。

验收：用户无需先打开业务应用，就能把桌面信息交给 AI，并明确知道发送了什么。

### 阶段三：长任务陪跑

能力：

- 可读步骤进度；
- 中断、重试和部分失败；
- 完成结果与后续动作；
- 低打扰任务收纳。

验收：用户能判断任务是在运行、等待自己还是失败，不再面对黑盒等待。

### 阶段四：低打扰办公搭子

能力：

- 专注模式；
- 话题聚合和去重；
- 每日/每周关键摘要；
- 可解释的打扰策略；
- 用户反馈驱动的优先级学习。

验收：MiniPet 主动出现更少，但用户处理关键事项更快；不以弹窗数量作为增长指标。

## 9. 产品成功标准

最终衡量 MiniPet 的不是“显示了多少消息”，而是：

- 主动打扰中有多少被用户认为值得；
- 从出现到完成决策需要多少步；
- 有多少任务无需切换应用即可确认或补充；
- 高风险动作是否始终透明、可控；
- 长任务是否有清晰状态和收口；
- 用户是否愿意让它长期留在桌面。

MiniPet 的核心原则始终是：普通信息留在原应用，AI 判断后的关键时刻才来到桌面。
