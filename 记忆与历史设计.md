# miniPet 自动总结设计

miniPet 不保存完整聊天历史。它只提供一个可选功能：**从最近对话里自动总结少量值得长期记住的重点**。这个功能可以在“设置 → 角色 → 自动总结”里开关。

## 当前规则

```text
当前对话：只在内存里临时保留，用来让本轮聊天接得上。
自动总结：每隔几次用户发言，从最近对话中提炼少量重点。
总结重点：保存到本地，用作以后聊天的长期上下文。
```

## 不做什么

miniPet 当前不做这些复杂功能：

- 不保存完整聊天历史。
- 不提供历史搜索。
- 不生成每日摘要。
- 不把旧聊天全文塞进大模型上下文。

## 什么会进入大模型

会进入：

1. 当前用户输入。
2. 当前运行中的最近几条对话。
3. 已保存的总结重点。

不会进入：

1. 完整聊天原文。
2. 已经被清空的当前对话。
3. 不值得长期保留的临时内容。

## 自动总结怎么工作

流程：

```text
用户聊天
→ 内存里保留最近几条消息
→ 达到总结频率
→ 大模型判断哪些信息值得长期记住
→ 保存为总结重点
→ 后续聊天把总结重点放进 system prompt
```

默认设置：

```text
每 3 次用户发言总结一次
每次参考最近 12 条消息
每次最多保存 3 条重点
```

## 总结重点保存在哪里

```text
data/memory/memories.json
```

设置页“角色”里可以查看、编辑、删除这些总结重点。

## 适合保存的内容

- 用户长期偏好。
- 用户希望宠物遵守的交流方式。
- 项目里未来还会用到的重要事实。
- 当前任务短期内还会继续用到的上下文。

## 不适合保存的内容

- 一次性寒暄。
- 临时情绪。
- 没有长期价值的小事。
- 随口说过但以后不需要的内容。

## 清空当前对话会怎样

清空当前对话只会清掉本次运行中的短期上下文。

不会删除已经保存的总结重点。

如果要删除总结重点，需要到“角色 → 总结重点管理”里删除。

## 最简理解

```text
不记流水账。
只在需要时，从最近聊天里总结几条真正有用的事。
以后聊天只带这些总结重点。
```
